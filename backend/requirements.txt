# Requirements for production & local development
#
# DIQQAT: bu fayl faqat QABUL QILINADIGAN oraliqni belgilaydi. Deploy uchun
# aynan qaysi versiya o'rnatilishini `requirements.lock` hal qiladi (u
# `pip-compile` bilan generatsiya qilinadi va Dockerfile o'shani o'rnatadi).
# Bu yerdagi versiyani o'zgartirgandan keyin lock'ni QAYTA generatsiya qiling:
#     pip-compile --generate-hashes -o requirements.lock requirements.txt
#
# Quyidagi minimallarning bir qismi xavfsizlik auditi natijasida ko'tarilgan —
# sababi har birining yonida.

# >=5.2.16: `UpdateCacheMiddleware` `Vary` sarlavhasiga `Authorization` ni
# qo'shmasligi (cache poisoning) va `DomainNameValidator` dagi sarlavha
# inyeksiyasi teshigi 5.2.15/5.2.16 da yopilgan.
Django>=5.2.16,<6.0
djangorestframework>=3.14
django-cors-headers>=4.0
psycopg[binary]>=3.1
djangorestframework-simplejwt>=5.3
# >=2.10 MAJBURIY (>=2.6 emas): accounts/views.py Google ID Token'ni mahalliy
# tekshirishda `jwt.decode(..., issuer=GOOGLE_ISSUERS)` ga bir NECHTA issuer
# (tuple) uzatadi. Bir nechta issuer'ni qabul qilish PyJWT 2.10.0 da qo'shilgan
# — undan oldingi versiyalarda `_validate_iss` oddiy `!=` solishtiruv qiladi,
# ya'ni tuple HECH QACHON mos kelmaydi va HAR BIR haqiqiy Google login
# `InvalidIssuerError` bilan 401 oladi. Bundan tashqari `PyJWKClient(timeout=)`
# 2.8.0 da qo'shilgan — 2.6/2.7 da modul import paytida TypeError beradi.
# `PyJWT>=2.6` pin'i bu ikkalasini ham qoplamasdi: simplejwt PyJWT'ni
# tranzitiv tortadi (`>=1.7.1,<3`), shuning uchun build keshida qolgan eski
# 2.8/2.9 pin'ni qanoatlantirib, pip uni YANGILAMASDI — natijada lokalda
# (yangi PyJWT) testlar o'tib, productionda Google login ishlamasdi.
PyJWT>=2.10
# >=12.3.0: decompression-bomb tekshiruvini chetlab o'tish teshiklari. Avatar
# yuklash oqimi (`accounts/views.py` `img.load()`) aynan shu tekshiruvga
# tayanadi, ya'ni teshik to'g'ridan-to'g'ri yetib boradi.
Pillow>=12.3.0
qrcode>=7.4
# >=6.14.2: maxsus tayyorlangan PDF cheksiz siklga / katta xotira sarfiga olib
# keladigan bir nechta DoS. Sink pdfplumber bilan bir xil: PDF import.
pypdf>=6.14.2
# >=0.11.10 MAJBURIY (>=0.10 emas): eski pin `pdfminer.six==20231228` ni
# tortardi, unda `CMapDB._load_data()` ISHONCHSIZ `pickle.loads()` ishlatadi
# (PYSEC-2026-1761/1762) — ya'ni maxsus tayyorlangan PDF kod ijrosiga olib
# kelishi mumkin. Sink `questions/pdf_generation.py` da
# `pdfplumber.open(io.BytesIO(pdf_bytes))` — bayt'lar to'g'ridan-to'g'ri
# foydalanuvchidan keladi va endpoint (`questions/views.py`
# `preview_pdf_questions`) platforma admini emas, HAR QANDAY tasdiqlangan
# o'qituvchi/manager uchun ochiq. 0.11.10 `pdfminer.six==20260107` ni
# pin qiladi (tuzatish 20251107/20251230 da chiqqan).
pdfplumber>=0.11.10
openpyxl>=3.1
python-docx>=1.0
reportlab>=4.0
celery>=5.3
redis>=5.0
django-celery-beat>=2.5
gunicorn>=21.2
whitenoise>=6.5
requests>=2.31
pyotp>=2.9
# >=46.0.7: sertifikat zanjirini tekshirishda DoS va `Hash.update()` da
# uzluksiz bo'lmagan bufer bilan buffer overflow. TOTP sirlarini Fernet bilan
# shifrlashda (`accounts/utils.py`) va JWT'da ishlatiladi.
cryptography>=46.0.7
cloudinary>=1.36
django-cloudinary-storage>=0.3
sentry-sdk[django]>=1.40
django-anymail[mailgun]>=10.0
gTTS>=2.4
django-axes>=6.0
pywebpush>=1.14
