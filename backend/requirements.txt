# Reproducible build uchun aniq versiyalar (lokal muhitda o'rnatilgan).
# Yangilashda: aktiv muhitda `pip freeze` chiqishini tekshirib, shu yerga ko'chiring.
Django==5.2.14
djangorestframework==3.17.1
django-cors-headers==4.9.0
psycopg[binary]==3.3.4
djangorestframework-simplejwt==5.5.1
Pillow==12.2.0
# Sertifikat va hisobotdagi haqiqiy QR kod (Pillow backend bilan rasm chizadi).
# Verify sahifasiga (prolymp.uz/certificates/verify/<uuid>) yo'naltiradi.
qrcode==8.2
pypdf==6.12.2
pdfplumber==0.11.4
openpyxl==3.1.5
# Excel/PDF eksport (markaz a'zolari, olimpiada natijalari).
reportlab==4.4.1
celery==5.3.6
redis==5.0.1
django-celery-beat==2.8.1
gunicorn==26.0.0
whitenoise==6.12.0
requests==2.34.2
# TOTP 2FA (admin/owner uchun ixtiyoriy ikki bosqichli autentifikatsiya).
pyotp==2.9.0
# TOTP maxfiy kalitini DB'da shifrlash (Fernet). To'g'ridan-to'g'ri ishlatiladi
# (accounts.utils.encrypt/decrypt_totp_secret) — transitive bog'liqlikka tayanmay
# aniq pinlanadi.
cryptography==46.0.5
# Cloudinary (faqat production'da, CLOUDINARY_CLOUD_NAME o'rnatilganda yoqiladi).
# Reproducible build uchun lokal venv'da o'rnatilgan versiyalarga pinlangan.
cloudinary==1.44.2
django-cloudinary-storage==0.3.0
# Sentry — xato monitoring (faqat SENTRY_DSN env o'rnatilganda yoqiladi).
sentry-sdk[django]==2.20.0
# Email yuborish — Mailgun backend (faqat MAILGUN_API_KEY o'rnatilganda
# yoqiladi, aks holda console/SMTP backend ishlatiladi).
django-anymail[mailgun]==12.0
# Admin login brute-force himoyasi (5 xato urinishdan keyin 1 soat lock).
# Pin: `>=6.0` deploy'dan deploy'ga boshqa versiya o'rnatib reproducible
# build'ni buzardi. 8.3.1 — hozirgi barqaror versiya (>=6.0 bilan so'nggi
# deploylar ham shu major'ni o'rnatgan).
django-axes==8.3.1
